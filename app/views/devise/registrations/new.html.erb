<%= render layout: 'devise/shared/panel', locals: {
  kicker: 'Mage account',
  title: 'Sign up',
  lede: 'Create an account to keep lists, gifts, and the online edition in one place.'
} do %>
  <div class="auth-form">
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <%= render 'devise/shared/error_messages', resource: resource %>

      <div class="field">
        <%= f.label :email %>
        <%= f.email_field :email, autofocus: true, autocomplete: 'email', required: true %>
      </div>

      <div class="field">
        <%= f.label :password %>
        <% if @minimum_password_length %>
          <span class="auth-hint"><%= @minimum_password_length %> characters minimum</span>
        <% end %>
        <%= f.password_field :password, autocomplete: 'new-password', required: true %>
      </div>

      <div class="field">
        <%= f.label :password_confirmation %>
        <%= f.password_field :password_confirmation, autocomplete: 'new-password', required: true %>
      </div>

      <div class="actions">
        <%= f.submit 'Sign up', class: 'auth-button auth-button-primary' %>
      </div>
    <% end %>
  </div>

  <%= render 'devise/shared/links' %>
<% end %>
