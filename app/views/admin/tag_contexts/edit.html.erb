<main class="tagging-admin">
  <h1>Edit <%= @tag_context.name %></h1>

  <%= form_with model: [:admin, @tag_context] do |form| %>
    <%= render "form", form: form %>
    <%= form.submit "Save style" %>
  <% end %>

  <p><%= link_to "Back to tagging", admin_tag_contexts_path %></p>
</main>
